/** @type {import('next').NextConfig} */
// const nextConfig = {};
const nextConfig = {
    basePath: '/slovene-step-by-step', // Replace 'repo-name' with your GitHub repository name
    assetPrefix: '/slovene-step-by-step/',
  };

export default nextConfig;
